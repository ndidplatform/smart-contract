version: '3.3'
services:
  tendermint_abci_did:
    build:
      context: ../
      dockerfile: docker/Dockerfile
      labels:
        build.commit: ${BUILD_COMMIT}
        build.date: ${BUILD_DATE}
    image: ${REPO_NAME:-ndidplatform}/tendermint_abci_did:${DOCKER_TAG:-latest}
